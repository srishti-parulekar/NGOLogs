use ngologs;

-- create table ngoLoginTables(user_ID int, ngo_ID int, foreign key(user_ID) references person(userID), foreign key (ngo_ID) references ngo(ngoId));

-- insert into ngoLoginTables values(2024004, 1424001);